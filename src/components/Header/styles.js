import styled from 'styled-components';

export const HeaderWrapper = styled.div`
         display: flex;
         width: 100%;
         background: #111;
         align-items: center;
         margin: 0 auto;
         padding: 10px 10%;
         box-sizing: border-box;
         flex-wrap: wrap;
       `;
