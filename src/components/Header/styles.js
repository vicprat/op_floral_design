import styled from 'styled-components';

export const HeaderWrapper = styled.div`
         display: flex;
         max-width: 1200px;
         align-items: center;
         margin: 0 auto;
         padding: 10px 20px;
         box-sizing: border-box;
         flex-wrap: wrap;
       `;
