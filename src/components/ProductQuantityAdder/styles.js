import styled from 'styled-components';
export const ProductQuantityAdderWrapper = styled.div`
         margin-top: 1rem;
         > strong {
           display: block;
           margin-bottom: 10px;
         }
         > form {
           display: grid;
         }
       `;
